import { SwitchDefaultDirective } from './switch-default.directive';

describe('SwitchDefaultDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchDefaultDirective();
    expect(directive).toBeTruthy();
  });
});
